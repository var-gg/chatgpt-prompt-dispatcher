param(
  [string]$TargetDir = "$HOME/.openclaw/skills/chatgpt-web-submit"
)

Write-Host "TODO: materialize portable skill bundle to $TargetDir"
Write-Host "Expected source: skill/"
Write-Host "Keep runtime state outside the bundle."
